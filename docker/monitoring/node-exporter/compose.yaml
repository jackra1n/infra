name: node-exporter

services:
  node-exporter:
    image: prom/node-exporter:v1.11.1
    container_name: node-exporter
    command:
      - --path.rootfs=/host
    network_mode: host
    pid: host
    volumes:
      - /:/host:ro
    restart: unless-stopped
